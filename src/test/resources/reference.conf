
server-ip="SERVER_IP"
server-ip=${?SERVER_IP}

tg-bot-api-token="BOT_TOKEN"
tg-bot-api-token=${?TG_BOT_API_TOKEN}

hlds-dir="HLDS_DIR"
hlds-dir=${?HLDS_DIR}

server-pool-size=2
server-pool-size=${?SERVER_POOL_SIZE}
